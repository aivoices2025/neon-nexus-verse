function TestApp() {
  return (
    <div>
      <h1>🎉 IT WORKS!</h1>
      <p>Your React app is running successfully!</p>
      <button onClick={() => alert('Button clicked!')}>
        Click me to test
      </button>
    </div>
  );
}

export default TestApp;